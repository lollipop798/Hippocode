# Project Profile (Fixture)

- 项目：Hippocode Recall Regression Fixture
- 目标：稳定回归 `incident priority`、`focusPath` 升级、`explicit full` 暴露
- 约束：默认 summary-first，不注入 full context
- 适用：`/hippo:recall` 最小回归数据集
